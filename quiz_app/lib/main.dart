import 'package:flutter/material.dart';
import 'package:quiz_app/quiz.dart';
/////import 'start_sceen.dart';
void main() {
  runApp(Quiz()); //TODO: Plaease don't forget to call the class name here
   /*  MaterialApp( 
      ///! This is the old redundant alternative to the above code
      home: Scaffold( 
        body: Container(
          decoration: BoxDecoration(
            gradient: const LinearGradient(
              colors: [Color(0xFF480366), Color(0xFFA60AF4)],
              begin: Alignment.bottomCenter,
              end: Alignment.topCenter,
            ),
          ),
          child: Startscreen(Quiz())))));} //! This is how you will call the function class 


 */
}
